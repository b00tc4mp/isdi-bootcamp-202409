# Servidor web

En esta aplicación hicimos una pequeña aproximación a un servidor web. 

La idea es ejecutar primero el servidor (index.js)

Y posteriormente ya podríamos ejecutar login o register respectivamente contra localhost:8080

El server renderizará los archivo html, y procesará los métodos post de los formularios cuando se ejecuten. 

