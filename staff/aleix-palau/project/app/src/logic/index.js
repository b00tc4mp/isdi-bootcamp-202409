import {
    registerUser,
    loginUser,
    getUserName,
    isUserLoggedIn,
    getUserId,
    logoutUser,
    updateUserProfile,
    getUserStage,
    updateUserStage,
    uploadUserPictures,
    getUserProfile
} from './users'

const logic = {
    registerUser,
    loginUser,
    getUserName,
    isUserLoggedIn,
    getUserId,
    logoutUser,
    updateUserProfile,
    getUserStage,
    updateUserStage,
    uploadUserPictures,
    getUserProfile
}

export default logic