import {
    registerUser,
    loginUser,
    getUserName,
    isUserLoggedIn,
    getUserId,
    logoutUser,
    updateUser,
    getUserStage,
    updateUserStage
} from './users'

const logic = {
    registerUser,
    loginUser,
    getUserName,
    isUserLoggedIn,
    getUserId,
    logoutUser,
    updateUser,
    getUserStage,
    updateUserStage
}

export default logic