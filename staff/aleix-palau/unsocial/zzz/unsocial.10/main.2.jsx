// deleted -> let loggedInUser = null

const rootElement = document.getElementById('root')
const root = ReactDOM.createRoot(rootElement) // it's in main.jsx of unsocial.11?

const { Component } = React















root.render(<App />)