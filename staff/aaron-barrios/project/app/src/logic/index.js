import {
    registerUser,
    loginUser,
    logoutUser,
    getUserId,
    getUserRole,
    getUserName,
    isUserLoggedIn
} from './users'

const logic = {
    registerUser,
    loginUser,
    logoutUser,
    getUserId,
    getUserRole,
    getUserName,
    isUserLoggedIn
}

export default logic