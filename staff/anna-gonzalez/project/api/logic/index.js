import {
    addPeriodEnd,
    createCycle,
    getPeriodDays
} from './cycles/index.js'

import {
    authenticateUser,
    getUserDetails,
    registerUser
} from './users/index.js'

const logic = {
    addPeriodEnd,
    createCycle,
    getPeriodDays,
    authenticateUser,
    getUserDetails,
    registerUser
}

export default logic