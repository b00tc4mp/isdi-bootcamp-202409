import {
    addPeriodEnd,
    createCycle,
    getLastCycleStart,
    getPeriodDays
} from './cycles/index.js'

import {
    authenticateUser,
    getUserDetails,
    registerUser
} from './users/index.js'

const logic = {
    addPeriodEnd,
    createCycle,
    getLastCycleStart,
    getPeriodDays,
    authenticateUser,
    getUserDetails,
    registerUser
}

export default logic