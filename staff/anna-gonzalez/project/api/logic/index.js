import {
    addPeriodEnd,
    createCycle,
    getCyclesStart,
    getCurrentCycleStart,
    getPeriodDays
} from './cycles/index.js'

import {
    authenticateUser,
    getUserDetails,
    registerUser
} from './users/index.js'

const logic = {
    addPeriodEnd,
    createCycle,
    getCyclesStart,
    getCurrentCycleStart,
    getPeriodDays,
    authenticateUser,
    getUserDetails,
    registerUser
}

export default logic