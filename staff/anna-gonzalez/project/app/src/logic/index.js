import {
    addPeriodEnd,
    createCycle,
    getLastCycleStart
    // getPeriodDays
} from './cycles'

import {
    getUserName,
    isUserLoggedIn,
    loginUser,
    logoutUser,
    registerUser
} from './users'

const logic = {
    addPeriodEnd,
    createCycle,
    getLastCycleStart,
    // getPeriodDays,
    getUserName,
    isUserLoggedIn,
    loginUser,
    logoutUser,
    registerUser
}

export default logic