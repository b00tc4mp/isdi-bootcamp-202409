import {
    isUserLoggedIn,
    loginUser,
    logoutUser,
    registerUser
} from './users'

const logic = {
    isUserLoggedIn,
    loginUser,
    logoutUser,
    registerUser
}

export default logic