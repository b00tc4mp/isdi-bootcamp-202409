import {
    addPeriodEnd,
    createCycle,
    getPeriodDays
} from './cycles'

import {
    getUserName,
    isUserLoggedIn,
    loginUser,
    logoutUser,
    registerUser
} from './users'

const logic = {
    addPeriodEnd,
    createCycle,
    getPeriodDays,
    getUserName,
    isUserLoggedIn,
    loginUser,
    logoutUser,
    registerUser
}

export default logic