import {
    createCycle
} from './cycles'

import {
    getUserName,
    isUserLoggedIn,
    loginUser,
    logoutUser,
    registerUser
} from './users'

const logic = {
    createCycle,
    getUserName,
    isUserLoggedIn,
    loginUser,
    logoutUser,
    registerUser
}

export default logic