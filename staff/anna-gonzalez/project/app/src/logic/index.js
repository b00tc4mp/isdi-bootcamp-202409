import {
    getUserName,
    isUserLoggedIn,
    loginUser,
    logoutUser,
    registerUser
} from './users'

const logic = {
    getUserName,
    isUserLoggedIn,
    loginUser,
    logoutUser,
    registerUser
}

export default logic