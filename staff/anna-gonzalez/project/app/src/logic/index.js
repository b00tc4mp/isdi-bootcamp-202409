import {
    addPeriodEnd,
    createCycle,
    getCyclesStart,
    getCurrentCycleStart,
    getPeriodDays
} from './cycles'

import {
    getUserName,
    isUserLoggedIn,
    loginUser,
    logoutUser,
    registerUser
} from './users'

const logic = {
    addPeriodEnd,
    createCycle,
    getCyclesStart,
    getCurrentCycleStart,
    getPeriodDays,
    getUserName,
    isUserLoggedIn,
    loginUser,
    logoutUser,
    registerUser
}

export default logic