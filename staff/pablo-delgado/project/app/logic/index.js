import {
    loginUser,
    logoutUser,
    registerUser,
    isUserLoggedIn,
    getUserId,
    getUserName
} from './users'

const logic = {
    loginUser,
    logoutUser,
    registerUser,
    isUserLoggedIn,
    getUserId,
    getUserName
}

export default logic