import {
    registerUser,
    loginUser,
    getUserName,
    isUserLoggedIn,
    isUserRoleModerator,
    isUserRoleRegular,
    getUserId,
    logoutUser
} from './users'

const logic = {
    registerUser,
    loginUser,
    getUserName,
    isUserLoggedIn,
    isUserRoleModerator,
    isUserRoleRegular,
    getUserId,
    logoutUser
}

export default logic