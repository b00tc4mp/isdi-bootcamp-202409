console.log('CASE locate broccoli from veggie')

var veggies = {0: 'broccoli', 1: 'cauliflower', 2: 'cabbage', 3: 'kale'}
var veggie = at(veggies, 3)
console.log(veggies)
//kale

console.log('CASE locate greeb from colors')
//

var colors = {0: 'red', 1: 'green', 2: 'blue', 3: 'yellow', length: 4}
var colors = at(colors, 1)
console.log(color)
//green


var at = function(iterable, index){
    // buscar en el iterable el elemento que se encuentra en el index//
    //always return undfine if index < -array.length or index >= array.length
}