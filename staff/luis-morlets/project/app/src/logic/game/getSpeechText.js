import { validate, errors } from "com"

export default text => {
    validate.text(text)

    let textLines = [
        { string: 'Welcome', speed: 70 },
        { string: 'Traveler', speed: 70 }
    ]
}