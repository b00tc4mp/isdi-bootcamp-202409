export default function Select({ children }) {
    return <select className='mt-1 w-full p-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500'>{children}</select>
}