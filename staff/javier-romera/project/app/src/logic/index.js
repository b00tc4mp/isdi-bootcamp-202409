import {
    registerUser,
    loginUser,
    isUserLoggedIn,
    logoutUser
} from './users'

const logic = {
    registerUser,
    loginUser,
    isUserLoggedIn,
    logoutUser
}

export default logic