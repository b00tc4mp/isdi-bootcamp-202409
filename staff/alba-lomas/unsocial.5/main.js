


var loggedInUser = null

var loginSection = buildLoginSection() // aqui le decimos que buildLoginSection es igual que loginSection.

var body = new Compo(document.querySelector('body'))
body.add(loginSection) // añadimos loginSection al body.