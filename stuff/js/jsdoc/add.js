/**
 * Adds two numbers and returns the result.
 * 
 * @param {number} a First operand. 
 * @param {number} b Second operand.
 * 
 * @returns The sum of first and second operands.
 */
function add(a, b) { return a + b }